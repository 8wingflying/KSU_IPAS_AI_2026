# 矩陣運算 20 題（含 Python 驗算程式）

## 📘 題目 + 參考解答 + Python 驗算

以下為 20 題矩陣運算題目，附完整解答與 Python 驗算程式（NumPy）。

------------------------------------------------------------------------

## 第 1 題：矩陣加法

A = \[\[1,2\],\[3,4\]\]\
B = \[\[5,-1\],\[2,0\]\]

### 解答

A + B =\
\[ egin{bmatrix}6 & 1  5 & 4\\end{bmatrix} \]

### Python 驗算

``` python
import numpy as np
A = np.array([[1,2],[3,4]])
B = np.array([[5,-1],[2,0]])
A + B
```

------------------------------------------------------------------------

## 第 2 題：矩陣減法

A - B =\
\[ egin{bmatrix}-4 & 3  1 & 4\\end{bmatrix} \]

``` python
A - B
```

------------------------------------------------------------------------

## 第 3 題：線性組合

2A - 3B =\
\[ egin{bmatrix}-13 & 7  0 & 8\\end{bmatrix} \]

``` python
2*A - 3*B
```

------------------------------------------------------------------------

## 第 4 題：標量乘法

-2C = \[\[2,-8\],\[0,-6\]\]\
3C = \[\[-3,12\],\[0,9\]\]

``` python
C = np.array([[-1,4],[0,3]])
-2*C, 3*C
```

------------------------------------------------------------------------

## 第 5 題：矩陣乘法 (2×2)

AB =\
\[ egin{bmatrix}2 & -1  6 & -1\\end{bmatrix} \]

``` python
A = np.array([[1,2],[3,4]])
B = np.array([[2,1],[0,-1]])
A @ B
```

------------------------------------------------------------------------

## 第 6 題：矩陣乘法 (2×3 × 3×1)

AB =\
\[ egin{bmatrix}-1  -1\\end{bmatrix} \]

``` python
A = np.array([[1,-1,2],[0,3,4]])
B = np.array([[2],[1],[-1]])
A @ B
```

------------------------------------------------------------------------

## 第 7 題：AB 與 BA

AB ≠ BA

``` python
A = np.array([[1,2],[0,1]])
B = np.array([[2,0],[1,2]])
A@B, B@A
```

------------------------------------------------------------------------

## 第 8 題：轉置

A\^T =\
\[ egin{bmatrix}1 & -1  2 & 0  3 & 4\\end{bmatrix} \]

``` python
A = np.array([[1,2,3],[-1,0,4]])
A.T
```

------------------------------------------------------------------------

## 第 9 題：行列式 (2×2)

det(A) = -5

``` python
A = np.array([[2,3],[1,-1]])
np.linalg.det(A)
```

------------------------------------------------------------------------

## 第 10 題：行列式 (3×3)

det(A) = 1

``` python
A = np.array([[1,2,0],[0,1,3],[2,-1,1]])
np.linalg.det(A)
```

------------------------------------------------------------------------

## 第 11 題：反矩陣

A⁻¹ =\
\[ egin{bmatrix}3 & -1  -5 & 2\\end{bmatrix} \]

``` python
A = np.array([[2,1],[5,3]])
np.linalg.inv(A)
```

------------------------------------------------------------------------

## 第 12 題：反矩陣求聯立方程

解得：x = -1, y = 3

``` python
A = np.array([[2,1],[5,3]])
B = np.array([1,4])
np.linalg.solve(A, B)
```

------------------------------------------------------------------------

## 第 13 題：列運算判斷

此系統有無限多解。

``` python
A = np.array([[1,2],[2,4]])
np.linalg.matrix_rank(A)
```

------------------------------------------------------------------------

## 第 14 題：矩陣秩

rank(A) = 2

``` python
A = np.array([[1,2,3],[2,4,6],[1,1,1]])
np.linalg.matrix_rank(A)
```

------------------------------------------------------------------------

## 第 15 題：tr(AB) = tr(BA)

兩者皆為 5

``` python
A = np.array([[1,2],[3,4]])
B = np.array([[0,1],[1,0]])
np.trace(A@B), np.trace(B@A)
```

------------------------------------------------------------------------

## 第 16 題：轉置性質

(AB)\^T = B\^T A\^T

``` python
(A@B).T, B.T @ A.T
```

------------------------------------------------------------------------

## 第 17 題：反矩陣轉置

(A⁻¹)\^T = (A\^T)⁻¹

``` python
A = np.array([[2,1],[1,1]])
np.linalg.inv(A).T, np.linalg.inv(A.T)
```

------------------------------------------------------------------------

## 第 18 題：冪等矩陣

A² = A

``` python
A = np.array([[1,0],[0,0]])
A @ A
```

------------------------------------------------------------------------

## 第 19 題：非冪零矩陣

A² = -I

``` python
A = np.array([[0,1],[-1,0]])
A@A
```

------------------------------------------------------------------------

## 第 20 題：多項式作用 p(A)=A²-3A+2I

結果：\
\[ egin{bmatrix}6 & -2  -3 & 6\\end{bmatrix} \]

``` python
A = np.array([[1,2],[3,1]])
I = np.eye(2)
A@A - 3*A + 2*I
```

------------------------------------------------------------------------

如需可下載 PDF 或加入更多題庫可告知！
